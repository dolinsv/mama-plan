{
  "name": "mama-plan-client",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist",
    "deploy:vk": "npm run build && node scripts/deploy-vk.mjs"
  },
  "dependencies": {
    "@vkontakte/vk-bridge": "^2.15.7",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "@vkontakte/vk-miniapps-deploy": "^1.0.2",
    "gh-pages": "^6.3.0",
    "vite": "^5.2.0"
  }
}
